{
  "devDependencies": {
    "loki": "^0.35.1"
  },
  "scripts": {
    "visual:report": "backstop openReport --config=backstop.config.json",
    "visual:test": "backstop test --config=backstop.config.json",
    "visual:approve": "backstop approve --config=backstop.config.json"
  }
}
